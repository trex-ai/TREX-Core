topic_aliases = {
    1: '/'.join([self.market_id, 'start_round']),
    # 2: '/'.join([self.market_id, '+', 'settled']),
    # 3: '/'.join([self.market_id, '+', 'bid']),
    # 4: '/'.join([self.market_id, '+', 'ask']),
    5: '/'.join([self.market_id, 'meter_data']),
    # 6: '/'.join([self.market_id, '+', 'return_extra_transactions'])
}